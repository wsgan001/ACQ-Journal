import csv
import Config
import numpy as np

def readFile(fileName):
    print(Config.localPath)
    
    csvFile = np.genfromtxt(fname=Config.localPath+fileName);
    reader = csv.reader(csvFile);
    print(reader[0:40]);
        
readFile("listening_history.csv");